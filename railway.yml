# railway.yml
name: my-python-app
services:
  - env: python=3.9
    install:
      - python3.9 -m pip install --upgrade pip
      - python3.9 -m pip install -r requirements.txt
    run: python3.9 run.py
